# Example environment variables for mytv_frontend
# Use .env.local for local overrides. Do not commit .env files.
# Note: Vite watcher ignores any .env* changes; restart manually after editing.

# VITE_API_BASE - optional base URL for API proxy (not used by default)
# VITE_ALLOWED_HOST - optional host allowance (defaults set in vite.config.js)
