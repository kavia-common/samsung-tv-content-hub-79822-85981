# Example environment variables for mytv_frontend
# Copy to .env.local for local development and adjust as needed.
# Vite will read .env.local; watcher is configured to ignore .env* changes to prevent reload storms.

# Server port for HMR client (optional). Default: 3000
# PORT=3000

# Example API base (if a backend is added later)
# VITE_API_BASE=/api
